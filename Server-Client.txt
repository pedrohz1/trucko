Client -> Server {
    roomList ()
    createRoom (roomName, playerName)
    joinRoom (roomName, playerName)
    startGame (roomName)

    playCard (player, card)
    challenge ()
}

Server -> Client{
    playCard (sucess, message, lastPlayedCard, biggestCard)
    endGame (winner)
}

rooms (roomName => game)

socket.data{
    socket.data.roomName = nome da sala
    socket.data.host = true / false
    socket.data.id = null / 1 / 2 / 3 / 4
    socket.data.team = null / 1 / 2 
    socekt.data.name = nome do jogador
}
