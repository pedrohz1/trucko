Client -> Server {
    roomList ()
    createRoom (roomName, playerName)
    joinRoom (roomName, playerName)
    startGame (roomName)

    playCard (player, card)
    challenge ()
}

Server -> Client{
    playCard (sucess, message, lastPlayedCard, biggestCard)
    endGame (winner)
}

rooms (roomName => game)

socket.data{
    socket.data.roomName
    socket.data.host
    socket.data.id
    socket.data.team
    socekt.data.name
}

